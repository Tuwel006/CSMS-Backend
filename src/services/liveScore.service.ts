import { LiveScorePayload } from "../types/score.type";
import { redisService } from "./redisScore.service";

export class LiveScoreService {
    // Implementation for live score service
    // static async buildPayload(matchId: string, inningsId: number): Promise<LiveScorePayload> {
    //     // await redisService.setScore({matchId, inningsId});
    // }
}